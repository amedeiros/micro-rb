syntax = "proto3";

package <%= @name %>.sum_handler;

service Sum {
    rpc Sum(Request) returns (Response) { }
}

message Request {
    int32 a = 1;
    int32 b = 2;
}

message Response {
    int32 total = 1;
}